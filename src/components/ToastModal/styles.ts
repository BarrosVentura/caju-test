import styled from "styled-components";

export const ModalWrapper = styled.div`
  padding-block: 14px;
  display: flex;
  flex-direction: column;
  gap: 14px;

  p {
    margin: 0;
    padding: 0;
  }

  > div {
    display: flex;
    gap: 12px;
  }
`;
